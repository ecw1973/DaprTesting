apiVersion: v1
kind: Service
metadata:
  name: {{ template "daprbackend.fullname" . }}
  labels:
    app: {{ template "daprbackend.name" . }}
    chart: {{ template "daprbackend.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "daprbackend.name" . }}
    release: {{ .Release.Name }}
